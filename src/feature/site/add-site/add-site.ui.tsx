import { Button } from "@/shared/ui/button";
import { FilePlus } from "lucide-react";

export function AddSiteButton() {
  // TODO: 로직 추가
  return (
    <Button>
      <FilePlus /> 사이트 추가하기
    </Button>
  );
}
